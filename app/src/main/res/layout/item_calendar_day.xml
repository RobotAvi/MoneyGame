<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="0dp"
    android:layout_height="70dp"
    android:layout_weight="1"
    android:layout_margin="2dp"
    android:background="@drawable/bg_cartoon_day"
    android:padding="8dp">

    <TextView
        android:id="@+id/tvDayNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="1"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/cartoon_text_primary"
        android:fontFamily="cursive"
        android:contentDescription="День календаря" />

    <ImageView
        android:id="@+id/ivPlayerToken"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="bottom|end"
        android:visibility="gone"
        android:src="@drawable/player_token"
        android:contentDescription="Токен игрока" />

</FrameLayout>